# I'm unsure if I need to make each directory individually
# Or if just doing the one line to make the parent and child would suffice
# I'll go for doing two lines and making each of them individually.

hdfs dfs -mkdir /holbies
hdfs dfs -mkdir /holbies/input